---
phase: 1
plan: 3
wave: 2
---

# Plan 1.3: Navigation Shell & Routing

## Objective
Set up GoRouter with a responsive bottom navigation bar shell layout.

## Context
- lib/core/router/
- lib/shared/widgets/

## Tasks

<task type="auto">
  <name>Implement Go Router</name>
  <files>lib/core/router/app_router.dart, lib/main.dart</files>
  <action>
    - Configure `GoRouter` as the application's primary navigation in `AppRouter`. 
    - Include predefined routes: `/splash`, `/login`, `/register`.
    - Set up a `ShellRoute` for the main dashboard features.
  </action>
  <verify>Navigating from splash to another path works.</verify>
  <done>GoRouter manages app state effectively.</done>
</task>

<task type="auto">
  <name>Create App Shell</name>
  <files>lib/shared/widgets/app_shell.dart</files>
  <action>
    - Develop an `AppShell` with a BottomNavigationBar containing tabs: Dashboard, Food, Workouts, Profile.
    - Provide placeholder screens under `features/` to display text corresponding to each tab.
  </action>
  <verify>Hot reloading and tapping tabs correctly shifts the displayed screen without breaking routes.</verify>
  <done>App Shell seamlessly transitions between features.</done>
</task>

## Success Criteria
- [ ] GoRouter configured
- [ ] Bottom navigation bar allows switching tabs
