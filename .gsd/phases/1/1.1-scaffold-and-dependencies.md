---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Scaffold Project & Dependencies

## Objective
Initialize the Flutter project natively in the root directory, establish the clean architecture folder structure, and install all required dependencies.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md

## Tasks

<task type="auto">
  <name>Create Flutter App</name>
  <action>
    - Run `flutter create .` in the root directory to initialize the Flutter app. (Supports Android and iOS).
  </action>
  <verify>Check that `pubspec.yaml` and `lib/main.dart` exist in the root directory.</verify>
  <done>Flutter project is initialized.</done>
</task>

<task type="auto">
  <name>Add Dependencies</name>
  <files>pubspec.yaml</files>
  <action>
    - Add required packages and their latest versions to `pubspec.yaml`:
      - `flutter_riverpod`, `hive`, `hive_flutter`, `pocketbase`, `pedometer`, `health`, `camera`, `image_picker`, `barcode_scan2`, `google_ml_kit`, `geolocator`, `flutter_map`, `youtube_player_flutter`, `fl_chart`, `razorpay_flutter`, `flutter_secure_storage`, `go_router`, `shared_preferences`, `connectivity_plus`.
    - Run `flutter pub get`.
  </action>
  <verify>`flutter pub get` completes successfully.</verify>
  <done>Dependencies are installed.</done>
</task>

<task type="auto">
  <name>Folder Structure</name>
  <action>
    - Create clean architecture directories under `lib/`: `core/`, `features/`, `shared/`.
    - Under `features/`, create placeholders for `auth`, `dashboard`, `food`, `workouts`, `karma`.
  </action>
  <verify>Check that `lib/core` and `lib/features/auth` exist.</verify>
  <done>Folder structure is established.</done>
</task>

## Success Criteria
- [ ] Flutter scaffolded natively in `f:\Fitness App`
- [ ] All required pubspec dependencies are resolved
- [ ] Clean architecture folder skeleton exists
